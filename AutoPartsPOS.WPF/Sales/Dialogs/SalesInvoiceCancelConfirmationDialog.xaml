<Window x:Class="AutoPartsPOS.WPF.Sales.Dialogs.SalesInvoiceCancelConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="تأكيد الإلغاء"
        Width="540"
        Height="300"
        AllowsTransparency="True"
        Background="Transparent"
        FlowDirection="RightToLeft"
        Language="ar-EG"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None">
    <Border Margin="18"
            Padding="24"
            Background="White"
            CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22" Opacity="0.24" ShadowDepth="5" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Width="26"
                                             Height="26"
                                             Margin="0,0,10,0"
                                             Foreground="{DynamicResource AppDangerBrush}"
                                             Kind="AlertCircleOutline" />
                    <TextBlock VerticalAlignment="Center"
                               FontSize="21"
                               FontWeight="SemiBold"
                               Text="تأكيد الإلغاء" />
                </StackPanel>
                <Button Grid.Column="1"
                        Width="34"
                        Height="34"
                        Padding="0"
                        Click="CloseButton_Click"
                        Content="✕"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </Grid>

            <TextBlock Grid.Row="1"
                       Margin="0,24,0,20"
                       FontSize="16"
                       LineHeight="27"
                       Text="هل أنت متأكد من أنك تريد إلغاء هذه الفاتورة؟&#x0a;&#x0a;هذه العملية ستقوم بإلغاء الفاتورة وإعادة الكميات إلى المخزون."
                       TextWrapping="Wrap" />

            <StackPanel Grid.Row="2"
                        HorizontalAlignment="Left"
                        Orientation="Horizontal">
                <Button MinWidth="155"
                        Click="ConfirmButton_Click"
                        Content="نعم، إلغاء الفاتورة"
                        IsDefault="True"
                        Style="{StaticResource DangerButtonStyle}" />
                <Button MinWidth="80"
                        Margin="8,0,0,0"
                        Click="CancelButton_Click"
                        Content="لا"
                        IsCancel="True"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
