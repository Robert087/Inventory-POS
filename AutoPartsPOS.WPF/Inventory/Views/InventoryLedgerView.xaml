<UserControl x:Class="AutoPartsPOS.WPF.Inventory.Views.InventoryLedgerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft"
             Language="ar-EG">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource ToolbarCardStyle}">
            <StackPanel>
                <Grid FlowDirection="LeftToRight">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" />
                    <TextBlock Grid.Column="1" HorizontalAlignment="Right" Style="{StaticResource PageTitleStyle}" Text="المخزون" />
                </Grid>

                <WrapPanel Margin="0,14,0,0">
                    <ComboBox Width="260"
                              DisplayMemberPath="NameAr"
                              ItemsSource="{Binding Products}"
                              SelectedItem="{Binding SelectedProduct}"
                              materialDesign:HintAssist.Hint="الصنف"
                              Style="{StaticResource ModernComboBoxStyle}" />
                    <DatePicker Width="170"
                                Margin="8,0,0,0"
                                SelectedDate="{Binding FromDate}"
                                materialDesign:HintAssist.Hint="من تاريخ"
                                Style="{StaticResource ModernDatePickerStyle}" />
                    <DatePicker Width="170"
                                Margin="8,0,0,0"
                                SelectedDate="{Binding ToDate}"
                                materialDesign:HintAssist.Hint="إلى تاريخ"
                                Style="{StaticResource ModernDatePickerStyle}" />
                    <Button Margin="8,0,0,0" Command="{Binding SearchCommand}" Style="{StaticResource SecondaryButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Margin="0,0,6,0" Kind="Magnify" />
                            <TextBlock Text="بحث" />
                        </StackPanel>
                    </Button>
                    <Button Margin="8,0,0,0" Command="{Binding ClearFiltersCommand}" Content="مسح الفلاتر" Style="{StaticResource SecondaryButtonStyle}" />
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource SectionCardStyle}">
            <Grid>
                <DataGrid x:Name="InventoryGrid"
                          IsReadOnly="True"
                          ItemsSource="{Binding Transactions}"
                          Style="{StaticResource ModernDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="2*" Binding="{Binding ProductNameAr}" Header="الصنف" />
                        <DataGridTextColumn Width="140" Binding="{Binding TransactionType}" Header="الحركة" />
                        <DataGridTextColumn Width="120" Binding="{Binding DisplayQuantity, StringFormat=F0}" Header="الكمية" />
                        <DataGridTextColumn Width="140" Binding="{Binding BalanceAfter, StringFormat=F0}" Header="الرصيد" />
                        <DataGridTextColumn Width="140" Binding="{Binding ReferenceType}" Header="نوع المرجع" />
                        <DataGridTextColumn Width="180" Binding="{Binding TransactionDate, StringFormat=g}" Header="التاريخ" />
                    </DataGrid.Columns>
                </DataGrid>

                <Border IsHitTestVisible="False">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource EmptyStateCardStyle}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=InventoryGrid, Path=Items.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="لا توجد حركات مخزون." />
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
